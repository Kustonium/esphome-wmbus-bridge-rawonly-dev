__pycache__/
*.pyc
*.pyo
.claude/
.agents/
_bmad/
_bmad\
_bmad-output/

# Local AI / BMAD workflow files
/.agents/
/.claude/
/_bmad/
/_bmad-output/
/_bmad-custom-backup-temp/
/CLAUDE.md
/docslocal/
/build/

# Local working notes, not part of the published component
/TODO-*.md

# Stale nested copy of the whole tree that lives in the synced folder.
# Root-anchored so it can never shadow a real path deeper in the repo.
/esphome-wmbus-bridge-rawonly-dev/

# Editor and tooling leftovers
*.bak
*.tmp
*.tmp.*

# ESPHome build output and local device configs
/.esphome/
# ...and anywhere else: tests/tools/ configs are compiled in place, which
# drops a build tree next to them that the rooted pattern above misses.
.esphome/
/secrets.yaml
