# ESPHome wM-Bus Bridge RAW-only — Heltec WiFi LoRa 32 V4 / SX1262
# EN: Commented example with optional LED blink and Heltec V4 FEM pins.
# PL: Przykład z komentarzami, opcjonalnym LED i pinami FEM Heltec V4.

substitutions:
  devicename: esphome-wmbus-heltec-v4
  friendly_name: "wMBus Bridge Heltec V4"

esphome:
  name: ${devicename}
  friendly_name: ${friendly_name}
  min_version: 2026.1.3
  name_add_mac_suffix: false

esp32:
  variant: esp32s3
  flash_size: 16MB
  cpu_frequency: 160MHz
  framework:
    type: esp-idf

psram:
  mode: quad

external_components:
  - source: github://Kustonium/esphome-wmbus-bridge-rawonly@main
    components: [wmbus_radio]
    refresh: 0s

logger:
  level: info
  logs:
    mqtt: warn
    component: warn

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  power_save_mode: none

mqtt:
  broker: !secret mqtt_broker
  username: !secret mqtt_user
  password: !secret mqtt_password
  # EN: plain MQTT on 1883; don't enable TLS unless your broker has a valid cert
  #     (a wrong/unknown cert blocks the ESP from connecting).
  # PL: zwykłe MQTT na 1883; nie włączaj TLS bez pewności co do certu brokera
  #     (zły/nieznany cert blokuje połączenie ESP).
  port: 1883
  skip_cert_cn_check: true

api:
  # Keep the Native API available (Home Assistant, esphome logs, OTA over API)
  # but do NOT let it reboot the board.
  #
  # Why: ESPHome defaults to reboot_timeout: 15min, and that timer restarts the
  # device when no Native API CLIENT is connected. A board that only publishes
  # to MQTT usually has no such client, so it reboots every 15 minutes forever.
  # That was measured here on 2026-08-20/21: the RX metadata showed 51 distinct
  # boot_id values per board in a single night, median 900 s between starts,
  # while Home Assistant had no ESPHome device added at all. After setting 0s
  # the same boards ran 14.1 h with one boot_id and a continuously rising seq.
  #
  # This is NOT mqtt.reboot_timeout - that one reacts to losing the broker and
  # is a separate decision. Leave it alone unless you have measured a broker
  # problem.
  reboot_timeout: 0s
ota:
  - platform: esphome
mdns:
  disabled: false
improv_serial:
# Reception timestamp: SNTP first, HA time as a fallback. If only
# `homeassistant` is listed and HA API is not connected, the clock
# never gets set and /rx frames go out without `received_at` - the
# add-on then reports "no timestamp" indefinitely. SNTP works from
# WiFi alone, so it recovers frames even before HA sees the board.
time:
  - platform: sntp
    id: sntp_time
    servers:
      - pl.pool.ntp.org
      - 0.pool.ntp.org
  - platform: homeassistant
    id: ha_time

# Optional basic ESP diagnostics.
debug:

spi:
  clk_pin: GPIO9
  mosi_pin: GPIO10
  miso_pin: GPIO11

# Optional status LED on Heltec V4, active-low.
output:
  - platform: gpio
    id: led_output
    pin:
      number: GPIO35
      inverted: true

light:
  - platform: binary
    id: status_led
    name: "LED"
    output: led_output
    restore_mode: ALWAYS_OFF

script:
  - id: blink_led
    mode: single
    then:
      - light.turn_on: status_led
      - delay: 50ms
      - light.turn_off: status_led

wmbus_radio:
  # ─────────────────────────────────────────────────────────────────────────
  # EN: REQUIRED here: radio_type, the board pins (cs/reset/busy/irq — or
  #     gdo0/gdo2 + cc1101_allow_experimental for CC1101), has_tcxo where the
  #     module has a TCXO, and listen_mode. Everything else below is OPTIONAL —
  #     sane defaults apply if you delete it: frequency, rx_gain,
  #     long_gfsk_packets, listen_mode_filter_after_parse, topic_name,
  #     diagnostic_*, highlight_meters and forward_meters.
  # PL: WYMAGANE tutaj: radio_type, piny płytki (cs/reset/busy/irq — lub
  #     gdo0/gdo2 + cc1101_allow_experimental dla CC1101), has_tcxo gdy moduł ma
  #     TCXO, oraz listen_mode. Cała reszta poniżej jest OPCJONALNA — po
  #     usunięciu działają sensowne domyślne: frequency, rx_gain,
  #     long_gfsk_packets, listen_mode_filter_after_parse, topic_name,
  #     diagnostic_*, highlight_meters oraz forward_meters.
  # ─────────────────────────────────────────────────────────────────────────
  radio_type: SX1262
  cs_pin: GPIO8
  reset_pin: GPIO12
  busy_pin: GPIO13
  irq_pin:
    number: GPIO14
    mode: input

  # Heltec V4 external FEM/LNA pins.
  fem_en_pin: GPIO2
  fem_ctrl_pin: GPIO7
  fem_pa_pin: GPIO46

  # dio2_rf_switch: true  # default: true
  has_tcxo: true
  # EN: DIO3 is a REGULATED output from the SX1262 itself, not a pass-through -
  #     wrong tcxo_voltage means the chip actively overdrives (or underdrives)
  #     whatever TCXO is wired to it. Heltec V4/XIAO both want 3.0V (the
  #     default), so this line can stay commented on those boards. A board
  #     whose TCXO wants something else - e.g. LilyGO T-Beam v1.2 at 1.8V,
  #     per Meshtastic's own variant.h - MUST set this, or risks damaging a
  #     TCXO rated well below what this chip would otherwise push onto it.
  # PL: DIO3 to wyjscie REGULOWANE przez sam SX1262, nie przelot - zle
  #     tcxo_voltage oznacza, ze chip aktywnie prze- albo niedostarcza napiecia
  #     do podlaczonego TCXO. Heltec V4/XIAO chca 3,0V (domyslne), wiec ta
  #     linia moze zostac zakomentowana na tych plytkach. Plytka, ktorej TCXO
  #     chce czegos innego - np. LilyGO T-Beam v1.2 na 1,8V, wg wlasnego
  #     variant.h Meshtastic - MUSI to ustawic, inaczej ryzykuje uszkodzenie
  #     TCXO ocenionego znacznie nizej niz to, co ten chip by mu inaczej dal.
  # tcxo_voltage: 3.0v  # default: 3.0v
  # EN: A TCXO board sets XOSC_START_ERR on every power-up as a matter of course:
  #     the chip tries to start its own crystal before DIO3 has been told to
  #     power the TCXO, and DIO3 is configured after reset. Clearing the flag
  #     once the reference is up is what the datasheet expects, and it is what
  #     turns the flag into a diagnostic: one that stays cleared was a power-on
  #     artefact, one that comes back after a clean clear is a reference that
  #     genuinely is not starting. Left off, the flag is always set on this board
  #     and therefore reports nothing at all.
  #     publish_dev_err_after_clear sends the re-read state to MQTT, which is the
  #     only way to see it on a node that receives nothing - and a node that
  #     receives nothing is exactly where the error register is the last thing
  #     left to read.
  # PL: Plytka z TCXO zapala XOSC_START_ERR przy kazdym starcie w sposob normalny:
  #     uklad probuje uruchomic wlasny kwarc, zanim DIO3 dostanie polecenie
  #     zasilenia TCXO, a DIO3 konfiguruje sie dopiero po resecie. Skasowanie
  #     flagi po ustawieniu referencji jest tym, czego oczekuje datasheet, i to
  #     dopiero czyni ja diagnostyczna: taka, ktora zostaje skasowana, byla
  #     artefaktem startu; taka, ktora wraca po czystym skasowaniu, to
  #     referencja, ktora naprawde nie startuje. Bez tego flaga jest na tej
  #     plytce zawsze zapalona i nie niesie zadnej informacji.
  #     publish_dev_err_after_clear wysyla ponownie odczytany stan na MQTT - to
  #     jedyny sposob, zeby zobaczyc go na wezle, ktory nic nie odbiera, a wlasnie
  #     tam rejestr bledow jest ostatnia rzecza, jaka zostala do odczytania.
  clear_device_errors_on_boot: true  # default: false
  publish_dev_err_after_clear: true  # default: false
  # rx_gain: boosted  # default: boosted
  long_gfsk_packets: true

  # EN: T1 receiver bandwidth. Affects T1 ONLY - C1 and S1 keep their measured
  #     234.3 kHz no matter what is set here.
  #     The 312 kHz default was inherited, never measured for T1, and is 25%
  #     wider than the window an SX1276 uses for the same mode, so it admits
  #     noise the signal does not need. 234khz is the value to compare against.
  #     Change it on ONE board and leave a second one at the default, otherwise
  #     the comparison says nothing.
  # PL: Szerokosc pasma odbiornika dla T1. Dotyczy WYLACZNIE T1 - C1 i S1
  #     zachowuja swoje zmierzone 234,3 kHz niezaleznie od tego ustawienia.
  #     Domyslne 312 kHz jest odziedziczone, nigdy nie zmierzone dla T1 i o 25%
  #     szersze niz okno SX1276 w tym samym trybie, wiec wpuszcza szum, ktorego
  #     sygnal nie potrzebuje. 234khz to wartosc do porownania.
  #     Zmieniac na JEDNEJ plytce, druga zostawic na domyslnej - inaczej
  #     porownanie niczego nie mowi.
  # sx1262_rx_bandwidth: 234khz  # default: 312khz

  # EN: Receive mode. Available values: t1, c1, both, s1.
  #     both means T1/C1 only. S1 is separate and cannot be combined with T1/C1.
  #     Default frequency: T1/C1/both = 868.950 MHz, S1 = 868.300 MHz.
  #     Optional frequency override example for S1 compatibility tests:
  #       frequency: 868.36
  # PL: Tryb odbioru. Dostępne wartości: t1, c1, both, s1.
  #     both oznacza tylko T1/C1. S1 jest osobny i nie łączy się z T1/C1.
  #     Domyślna częstotliwość: T1/C1/both = 868.950 MHz, S1 = 868.300 MHz.
  #     Przykład opcjonalnego nadpisania częstotliwości dla testów S1:
  #       frequency: 868.36
  listen_mode: t1
  # listen_mode_filter_after_parse: false  # default: false

  # MQTT topics are generated automatically.
  # topic_name is optional; if omitted, esphome.name is used.
  # Generated topics:
  #   wmbus/<topic_name>/telegram
  #   wmbus/<topic_name>/diag/...
  # topic_name: "${devicename}"


  diagnostic_mode: normal
  # diagnostic_summary_interval: 60s  # default: 60s


  # EN: Publish the last measured RSSI for each forwarded meter as a retained
  #     integer at wmbus/<topic_name>/rssi/<meter_id>. Frames without a real
  #     RSSI measurement are skipped, so no placeholder value is ever sent.
  #     The wMBus MQTT Bridge add-on turns it into one signal-strength entity
  #     per receiving board, which is how two boards can be compared.
  # PL: Publikuj ostatnie zmierzone RSSI każdego przekazanego licznika jako
  #     zachowaną liczbę całkowitą na wmbus/<topic_name>/rssi/<meter_id>.
  #     Ramki bez rzeczywistego pomiaru są pomijane, więc żadna wartość
  #     zastępcza nie jest wysyłana. Dodatek wMBus MQTT Bridge robi z tego
  #     osobną encję siły sygnału dla każdej płytki, co pozwala je porównać.
  # publish_rssi: true  # default: false

  # Optional 15-minute per-meter statistics for selected IDs.
  # highlight_meters:  # default: []
  #   - "00088888"
  #   - "03500001"
  # highlight_ansi: true  # default: false
  # highlight_tag: "wmbus_user"  # default: wmbus_user
  # highlight_prefix: "★ "  # default: "★ "

  # EN: Optional whitelist — only these meters reach wmbus/<topic_name>/telegram.
  #     Everything else is dropped on the ESP, which is the point in a dense block.
  #     Use `forward_meters: true` instead of a list to reuse highlight_meters above.
  #     Quote hex IDs, otherwise YAML turns 0x417F0666 into the number 1098843750.
  # PL: Opcjonalna whitelista — na wmbus/<topic_name>/telegram trafiają tylko te liczniki.
  #     Reszta jest odrzucana na ESP, o co chodzi w gęstej zabudowie.
  #     Zamiast listy można dać `forward_meters: true` i użyć listy z highlight_meters.
  #     ID szesnastkowe w cudzysłowie, inaczej YAML zamieni 0x417F0666 na 1098843750.
  # forward_meters:  # default: []
  #   - "00088888"
  #   - "0x417F0666"

  # diagnostic_publish_summary_15min: true

  # Optional experimental/aggressive parser-side filter.
  # Use only for A/B tests and compare meter_snapshot.
  # listen_mode_filter_after_parse: true  # default: false

  on_frame:
    then:
      - script.execute: blink_led
